{% extends 'layout.twig' %}

{% block main_title %}
    Login
{% endblock main_title %}

{% block body %}
    <div class="container">
        <div class="row page">
            <div class="col-sm-12">
                <h3 class="section-title title">{{ page.title }}</h3>
            </div>
            {{ include("_notifications.twig") }}
            <div class="col-sm-12">
                {% include "_recover-form.twig" %}
            </div>
        </div>
    </div>
{% endblock body %}
