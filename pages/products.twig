{% extends 'layout.twig' %}

{% block body %}
<div class="featured-products pad">
  <div class="container">
    <div class="row">
      <div class="col-xs-12 c-brand-h">
        <h1 class="c-brand upper">Productos</h1>
      </div>
    </div>
    {% if products.count == 0 %}
      <div class="row">
        <div class="col-sm-12">
          <div class="pad">
            <div class="placeholder-help">
              <div class="placeholder-helper">
                <div class="placeholder-info">
                  <p>Todavía no hay productos, sin embargo, una vez que comiences a agregar cada uno de ellos, éstos se registrarán aquí sin importar si ya aparecen en existencia dentro de la colección.</p>
                  <a href="/admin/products" class="btn btn-placeholder">Agregar Productos</a>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    {% else %}
      <div class="row">
        <div class="col-xs-12 featured-products">
          {% for products_row in products | batch(4) %}
          <div class="row">
            {% for product in products_row %}
              <div class="col-xs-12 col-sm-6 col-md-3">
                {% include "_product-card.twig" %}
              </div>
            {% endfor %}
          </div>
          {% endfor %}
        </div>
      </div>

      {% include '_pagination.twig' %}

    {% endif %}
  </div>
</div>
{% endblock %}
